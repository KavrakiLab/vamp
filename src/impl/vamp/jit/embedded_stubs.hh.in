// AUTO-GENERATED by CMake from embedded_stubs.hh.in.
//
// Embeds vamp's JIT stub sources as constexpr string_views so the runtime
// doesn't carry a giant raw-string-literal blob inline. Editing the stub
// C++ now happens in templates/*.cc next to the rest of the JIT code,
// gets normal syntax highlighting and clang-format treatment.
//
// The {{robot_name}} / {{rake}} / {{resolution}} / {{planner_*}} placeholders
// inside the templates are runtime substitutions handled by inja in
// vamp::jit::generate_stub_source — CMake leaves them untouched
// (configure_file's @ONLY mode only touches @VAR@).
#pragma once

#include <string_view>

namespace vamp::jit::embedded
{
    inline constexpr std::string_view preamble = R"VAMPJIT_DELIM(@VAMP_JIT_PREAMBLE@)VAMPJIT_DELIM";
    inline constexpr std::string_view sampler_stub = R"VAMPJIT_DELIM(@VAMP_JIT_SAMPLER_STUB@)VAMPJIT_DELIM";
    inline constexpr std::string_view simplify_stub = R"VAMPJIT_DELIM(@VAMP_JIT_SIMPLIFY_STUB@)VAMPJIT_DELIM";
    inline constexpr std::string_view debug_stub = R"VAMPJIT_DELIM(@VAMP_JIT_DEBUG_STUB@)VAMPJIT_DELIM";
    inline constexpr std::string_view planner_stub = R"VAMPJIT_DELIM(@VAMP_JIT_PLANNER_STUB@)VAMPJIT_DELIM";
}  // namespace vamp::jit::embedded
